// components.js — 组件注册入口 (Phase 3 预留)
// Custom Elements 将在此注册

export function registerComponents() {
  // TODO: Phase 3 — iOS / watchOS / Web 组件
  console.log('[components] Component registry initialized');
}
